# To duplicate with .env.local.
# If you prefer you can setup the environment variables through the convex dashboard

# Set Convex environment variables
# bunx convex env set BETTER_AUTH_SECRET=$(openssl rand -base64 32)
# bunx convex env set SITE_URL "https://xxx.convex.site"
# bunx convex env set WEB_APP_ORIGIN "http://localhost:5173"

# SITE_URL: Convex HTTP site URL (NOT the frontend URL)
# - Cloud: https://xxx.convex.site (from Convex dashboard, ends in .site)
# - Local: http://127.0.0.1:3211 (Convex dev server)
VITE_CONVEX_SITE_URL=
SITE_URL=

# Deployment used by `npx convex dev`
CONVEX_DEPLOYMENT=dev:......

CONVEX_URL=

# npx convex env set TWITCH_CLIENT_ID=......
TWITCH_CLIENT_ID=
# npx convex env set TWITCH_CLIENT_SECRET=......
TWITCH_CLIENT_SECRET=